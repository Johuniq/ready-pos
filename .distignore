# Development and build files
.git
.github
.gitignore
.editorconfig
.prettierignore
.prettierrc.json
.phpcs.xml.dist
.kiro
.puku
.vscode
.cursor
.wordpress-org

# Node and package management
node_modules
package-lock.json
package.json
npm-debug.log
yarn.lock
yarn-error.log
pnpm-lock.yaml

# Source files (pre-compiled)
src

# Build configuration
vite.*.config.js
postcss.config.cjs
tailwind.config.js
tsconfig.json
components.json
gruntfile.cjs

# Documentation and tests
tests
documentation
bin
*.md
!readme.txt
DEPLOYMENT.md
TROUBLESHOOTING.md

# Markdown files (keep readme.txt only)
*.md
!README.md

# Release artifacts
release

# Development files
plugin-config.json
**/vite-dev-server.json

# OS files
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Composer dev dependencies
# Note: vendor/ is NOT excluded - it must be included in distribution
# Run 'composer install --no-dev' before building the release package
# The 'npm run release' command handles this automatically
# composer.json is INCLUDED so users can see dependency information
composer.lock
